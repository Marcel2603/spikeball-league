# Tools

Tools are go-scripts that help your local development. They will not be bundled to spikeball-league.

## Requirements

- Create a folder under `tools`
- Exclude it from go build by adding `//go:build spikeball-league_tools` as first line
