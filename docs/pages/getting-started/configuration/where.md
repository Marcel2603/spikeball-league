# Where to configure

## YAML Files

The service merges configuration from two YAML files at startup:

### `app.default.yml`

Bundled inside the binary — provides safe defaults. **Do not edit this file directly.**

### `app.yaml` / `app.yml`

Your user-supplied override file. Place it next to the binary (or mount it at `/opt/spikeball-league/app.yaml` in Docker).
Only the keys you specify will take effect — the rest fall back to `app.default.yml`. The system will automatically
detect either `app.yaml` or `app.yml`.

**Example minimal `app.yaml`:**

```yaml
server:
  port: 8080
```

---

## Environment Variables

Every configuration key can be overridden via an environment variable. The variable name is derived by
uppercasing the YAML path and joining segments with `_`.

| YAML key               | Environment variable        |
|------------------------|----------------------------|
| `server.port`          | `SERVER_PORT`              |
| `server.host`          | `SERVER_HOST`              |
| `server.domain`        | `SERVER_DOMAIN`            |
| `log.level`            | `LOG_LEVEL`                |
| `database.path`        | `DATABASE_PATH`            |

**Example Docker run with environment variables:**

```bash
docker run \
  -p 3000:3000 \
  ghcr.io/marcel2603/spikeball-league/spikeball-league:latest
```
