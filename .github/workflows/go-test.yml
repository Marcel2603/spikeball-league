name: Test & Lint
on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
  pull_request :
    paths-ignore:
      - 'docs/**'
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.26.x'
      - name: Lint
        run: make lint
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.26.x'
      - name: Test with the Go CLI
        run: make cover
